
{% extends 'base.html.twig' %}

{% block title %}
Creation nouvel article 
{% endblock %}

{% form_theme formPost 'tailwind_2_layout.html.twig' %}

{% block body %}

{% for label, messages in app.flashes(['success', 'warning']) %}
    {% for message in messages %}
        {# <div class="flash-{{ label }}">
            {{ message }}
        </div> #}
        <div role="alert" class="rounded border-s-4 border-teal-500 bg-teal-50 p-4">
  <strong class="block font-medium text-teal-800"> {{ label }} </strong>

  <p class="mt-2 text-sm text-teal-700">
    {{ message }}
  </p>
</div>
    {% endfor %}
{% endfor %}

<button class="w-full
   bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded  self-end"
   id="coucou">
  coucou
</button>

<div class="relative w-[50dvw] bg-blue-100 mx-auto my-12 p-5">

  {{form_start(formPost)}}
      <h2 class="text-center"> Créer un article</h2>
  {{ form_widget(formPost) }}
    <button class=" w-full
    bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
    Button
    </button>
  {{form_end(formPost)}}
</div>

{% endblock %}